#pragma once

#include <cstddef>

namespace mir {
    static constexpr std::size_t DEFAULT_ARRAY_CAPACITY = 1024;
    static constexpr std::size_t DEFAULT_POOL_CAPACITY = 1024;
    static constexpr std::size_t DEFAULT_SPARSE_SET_CAPACITY = 1024;
    static constexpr std::size_t DEFAULT_MAP_CAPACITY = 1024;
    static constexpr std::size_t DEFAULT_STRING_CAPACITY = 256;
}
